/**
 * Centralized log file paths for the application.
 */
const logDirectory = "logs";

export const LOG_PATHS = {
  LOG_ACCESS: `${logDirectory}/access.log`,
  LOG_UMATOMA: `${logDirectory}/feature/umatoma.log`,
};
